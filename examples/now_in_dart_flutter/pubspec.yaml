name: now_in_dart_flutter
description: A simple app that uses WebView under the hood to display all the news and updates regarding Dart and Flutter. The information is retrieved using GitHub's API and the github repository used as source is that of Flutter and Dart's.

publish_to: 'none'

version: 2.0.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.3.2
  equatable: ^2.0.5
  flash: ^3.0.5+1
  flutter_bloc: ^8.1.3
  flutter_svg: ^2.0.7
  fpdart: ^1.1.0
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  meta: ^1.9.1
  url_launcher: ^6.1.12
  webview_flutter: ^4.2.3
  path_provider: ^2.1.0
  vector_graphics: ^1.1.7

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^2.0.2
  mocktail: ^1.0.0
  test: ^1.24.6
  very_good_analysis: ^5.0.0+1

flutter:
  uses-material-design: true
  assets:
    - assets/
